# Tiptap 编辑器体验优化验证矩阵

- 日期：2026-08-04
- 本轮结论：阶段 1-2 的代码范围通过静态回归；未执行浏览器 E2E、视觉截图、axe、历史 fixture 与性能验证。
- 2026-08-05 更新：主工具栏命令条横向滚动（ADR-0005）落地；共享包 8 项回归测试通过、桌面 `tsc --noEmit` 与目标文件 ESLint 通过。

| 范围 | 场景 | 预期 | 状态 | 当前证据 |
|---|---|---|---|---|
| 命令注册表 | 同一粗体命令出现在主栏与 Bubble Menu | active/disabled/execute 结果一致 | 已验证 | `packages/tiptap-editor/tests/editor-command-registry.test.ts`，2026-08-04 通过 |
| 主工具栏 | 桌面常用宽度 | 命令条完整展示，不出现滚动条 | 部分验证 | 源码契约测试通过；缺 Playwright 截图 |
| 主工具栏 | 320-360px 窄屏 | 命令条横向滚动可达全部命令（滚动条隐藏），固定组不裁切 | 部分验证 | 滚动条隐藏与源码契约通过（2026-08-05 更新）；缺真实视口验证 |
| 插入/格式菜单 | 打开、切换、点击外部、Esc | 同时仅一个菜单打开，关闭后焦点可恢复 | 部分验证 | ESLint 与源码契约通过；缺 DOM 焦点测试 |
| 菜单键盘 | 方向键、Home/End、原生 input/select | 菜单项可导航，原生控件按键不被劫持 | 部分验证 | `editor-toolbar-contract.test.ts` 覆盖源码契约；缺组件测试 |
| Bubble Menu | 选择普通文本 | 正确定位并复用注册命令 | 部分验证 | 官方 `BubbleMenu` 已接入；排除 image/media/codeBlock；缺 E2E |
| Floating Menu | 顶层空段落 | 仅在顶层空段落出现 | 部分验证 | `$from.depth === 1` 契约已覆盖；缺编辑器集成测试 |
| Drag Handle | 普通块拖拽 | 使用官方 Drag Handle 且 AI 锁定时隐藏 | 部分验证 | 静态接入完成；缺 transaction/history E2E |
| Slash Menu | 输入 `/` 并搜索中英文关键词 | 过滤、分组、方向键、Enter、Esc 正常 | 未实施 | 当前仅保留 `slash` surface 元数据，无 Slash UI |
| 块菜单 | 点击手柄 | 显示转换、复制、删除、前后插入 | 未实施 | 当前手柄仅提供拖拽 |
| 图片上传 | 选择、粘贴、拖放图片 | 进入同一上传占位流程 | 未实施 | 仅保留既有粘贴回调，无统一占位/进度/重试 |
| 图片 URL | 从插入菜单输入 URL | 输入聚焦稳定，Enter 确认，Esc 只关闭内层输入 | 部分验证 | 回归修正与源码契约通过；缺 DOM 测试 |
| 媒体嵌入 | 从命令菜单插入媒体 | 有真实执行器与反馈 | 未实施 | 无执行器的菜单描述已移除；既有粘贴转换保留 |
| 链接 | Cmd/Ctrl+K 创建、编辑、移除 | 同一 Popover 与命令语义 | 未实施 | 当前仍为 Bubble Menu 基础 URL 输入 |
| 链接安全 | `javascript:` 等危险协议 | 拒绝插入并显示可理解反馈 | 未验证 | 缺协议策略单元测试 |
| 表格 | 网格插入不同尺寸 | 行列正确，Tab 导航正常 | 未实施 | 当前仍插入固定 3x3 |
| 表格上下文 | 增删行列、表头、合并拆分 | `editor.can()` 与执行结果一致 | 未实施 | 无上下文工具 |
| 粘贴 | 从网页/Word 粘贴 | 保留语义，清除外部视觉样式 | 需求冲突 | 当前扩展仍保留字体/字号/颜色/背景；需单独决策与 fixture |
| 保存状态 | dirty/saving/saved/error | Web/Desktop、Story/Blog 展示一致 | 未实施 | 无共享 `SaveState` 契约 |
| AI 上下文 | 从选区/块菜单调用 AI | 展开侧栏并携带准确上下文 | 未实施 | 当前 Bubble/块表面无 AI 命令 |
| AI 锁定 | 任务执行期间阻止编辑入口 | 所有变更入口被阻止，任务结束解锁 | 已回归 | `ai-task-lock*.test.ts` 与相关回归测试，2026-08-04 通过 |
| 旧内容/归一化 | 历史 HTML/JSON、自定义节点、重复归一化 | 无静默丢失且归一化幂等 | 未实施 | 缺 schema version、诊断和 fixture |
| 无障碍 | Alt+F10、方向键、Esc、ARIA | 键盘主流程可完成，无焦点陷阱 | 部分验证 | 源码契约与 ESLint 通过；缺 axe/手动键盘/组件测试 |
| 主题与宿主 | Web/Desktop 明暗主题及四宿主 | 浮层清晰，命令结果一致 | 未验证 | Desktop TypeScript 通过；缺四宿主运行验证 |
| 性能 | 长文档、图片和表格混合内容 | 输入与菜单打开无明显延迟 | 未验证 | 缺性能基准 |

## 2026-08-04 已执行命令

1. 目标文件 ESLint：通过。
2. 共享包回归测试：7 项通过，0 失败。
3. Desktop `tsc --noEmit`：通过。
4. 目标范围 `git diff --check`：使用 `cr-at-eol` 识别仓库既有 CRLF 后通过。
5. 根项目 `tsc --noEmit`：仍被任务外既有类型错误阻塞，不作为本轮 Tiptap 范围通过证据。

## 后续质量门槛

1. Slash、块菜单、上传、链接、表格、保存状态和内容兼容必须在各自实现后补编辑器集成测试。
2. 补 Playwright 桌面宽屏、桌面窄窗口、320-360px 移动宽度和触控模拟证据。
3. 补 DOM 焦点/键盘测试与 axe 扫描，替代仅依赖源码正则的契约测试。
4. 使用真实历史内容 fixture 验证编辑态、阅读态、序列化与幂等归一化。
5. React 19 下完成共享包严格模式、焦点与 hydration 验证后，再宣告完整规格验收。
