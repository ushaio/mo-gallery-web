# Flutter 前端 Spec 索引

MO Gallery Flutter app（`flutter/`，嵌套独立 git 仓库）的编码契约。UI 硬规范见 `.agents/skills/flutter-gallery-ui/SKILL.md`（kit 组件、AppIcons、tokens、strings.dart，禁 Material 原生）。

## 文档

- [auth-flow.md](auth-flow.md) — 两级认证门禁（Emulsion 官方账号 + 站点连接）的路由契约、存储 key 与端点对齐基准。
