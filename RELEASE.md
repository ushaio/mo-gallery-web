# 更新日志

## v0.8.0 - 2026-08-04

### 📱 移动端首发（Flutter / Android）
- 新增基于 Flutter 的 Android-first 移动上传客户端：登录、上传、照片浏览核心流程。
- 扩展移动端画廊浏览体验。
- 本次随 v0.8.0 发布首个 Android APK 安装包，后续将随 GitHub Release 自动产出。

### 📓 Zine 画册编辑器（桌面端）
- 新增 Zine 领域模型与本地草稿存储，支持自动保存与脏状态守护。
- 新增 Zine 仪表盘路由与画布编辑外壳、模板画廊。
- 照片托盘与槽位编辑：槽位变换、图片加载状态管理、图片资源本地化。
- 画布缩放控制、按屏幕物理像素渲染、视口自适应。
- 支持按物理尺寸导出 PDF。
- 增强 Zine AI 助手工作流。

### 🤖 AI 编辑器工作流（Web + 桌面）
- 集成 AI 工作区（AI Workspace），Web 与桌面端共享同一套编辑器 AI 能力。
- Web 叙事编辑器支持直接 AI 编辑（direct-edit）编排。
- AI 对话安全持久化、受守卫的 AI 任务会话，避免并发与中断问题。
- AI 任务与生成图片持久化保存，桌面端重启后可恢复。
- 编辑器包跨端共享（Tiptap 等依赖统一对齐）。

### 🗂 桌面端本地资源库
- 新增本地资源库 MVP 工作流：文件夹规划、查询令牌。
- 新增本地资源库全文搜索。
- 新增本地数据库恢复与自动备份。
- 资源库工作流持续扩展，能力与 Web 资源库对齐。

### 🖥 桌面端 UI 重构（设计改动较多）
- 设置页重设计：模型配置采用 master-detail 布局，持久化选中模型源。
- 存储整理页改为桌面 master-detail 布局。
- 胶卷管理页采用 master-detail 布局，与照片库样式保持一致。
- 照片库管理页参照本地资源库：左右两栏 + 右侧信息栏 + 双击大图。
- 照片信息栏统一分区式设计，新增 i18n 分区标签。
- 资源库左侧分区支持折叠且状态持久化，展开/折叠状态跨页面保留。
- 资源库统一承载云端与本地能力，通过顶栏标签切换。
- 新增桌面窗口框架（DesktopWindowFrame）与侧边栏重构。
- 新增统一 SelectDropdown 组件，本地库工作台、资产网格、筛选、详情、组织导航全面重构。
- 上传页布局与 Web 端对齐，缓存管理模块重构（app-cache）。

### 🖼 相册、照片与 Web
- 新增相册位置管理，跨 Web / 桌面 / 移动端同步。
- 保留胶片卷格式类型；修复 admin 照片页无效自定义属性。
- Web 画廊浏览与照片投放优化，故事时间线滚动修复。
- 移除照片网格 hover 缩放，修复 masonry 布局缓存重建。
- 补全国际化键（common.all 等），中文环境下筛选器显示「全部」。

### 🛠 其他
- README 更新项目架构说明；移除失效的本地工具配置。
- 桌面端登录错误详情展示。

## v0.7.0-beta - 2026-07-03

### 🖥 桌面端重大更新
- 新增基于 Go + Wails 的 MO Gallery Desktop 桌面管理端。
- 桌面端补齐概览、照片、故事、博客、存储、设置等核心管理工作流，侧边栏、国际化、页面组件与 Wails bindings 持续完善。
- 新增桌面端 AI 助手与 AI 图片生成工作流，支持 AI 模型配置、API Key 显示/隐藏、模型设置界面优化。
- 桌面端上传体验大幅增强：支持数码/胶片上传模式、完整上传参数、拖拽导入、上传页布局与 Web 端对齐。
- 强化桌面端登录状态：支持会话持久化与校验、认证失败处理、退出登录二次确认。

### 📸 上传与图片处理
- 上传参数组件重构为可复用结构，统一数码/胶片上传参数与压缩模式。
- 默认启用图片压缩，新增 WASM AVIF 压缩能力，并将 AVIF 编码迁移到 Web Worker，避免浏览器主线程卡顿。
- 优化上传进度展示：完善重试逻辑、进度报告、压缩后文件名、原始文件名到存储文件名的提示与 tooltip 展示。
- 修复上传路径重复、目标文件名初始化、上传完成后文件名展示等问题。
- 新增本地存储路径穿越保护，提升上传与存储安全性。

### ✍️ 故事编辑与富文本
- 支持保存 TipTap JSON 内容，提升富文本内容结构化存储能力。
- 改进故事编辑器草稿管理、新建故事草稿流程与粘贴上传占位符替换。
- 编辑故事时上传弹窗可自动关联当前故事，并在每次进入编辑页时要求确认粘贴上传设置。
- 抽离粘贴上传占位符工具逻辑，提升编辑器上传流程稳定性。

### 🖼 相册、图库与照片管理
- 新增精选图库页面。
- 新增照片图库可见性字段，并支持批量更新照片图库显示状态。
- 优化后台照片网格加载与交互：新增照片注册接口、懒加载网格、照片网格滑块性能优化。
- 默认后台照片按拍摄时间倒序排列。
- 胶片灯箱新增上一张/下一张导航。

### 🛠 后台体验与性能优化
- 后台偏好设置与会话缓存集中管理。
- 优化后台设置、友链页面、照片批量操作布局、滚动阴影、筛选折叠动画与 Cmd+K 提示。
- 调整批量照片操作为侧边面板/折叠面板布局，减少界面拥挤。
- 修复 `content-visibility` 导致的空白闪烁问题。

### 🐛 修复与基础设施
- 修复 Prisma 时间区扩展导致日期错误偏移的问题。
- Vercel 构建改用 webpack，提升构建兼容性。
- 清理无效代码、更新本地工具配置与环境变量示例。
