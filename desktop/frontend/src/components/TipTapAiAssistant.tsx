/**
 * TipTapAiAssistant — stub，desktop 端 AI 助手独立页面实现。
 */
export default function TipTapAiAssistant(_props: {
  options?: any
  context?: any
  onSyncContext?: () => void
  onApplyResult?: (mode: 'replace' | 'insert' | 'append', text: string, selectionRange?: any) => void
}) {
  return null
}
